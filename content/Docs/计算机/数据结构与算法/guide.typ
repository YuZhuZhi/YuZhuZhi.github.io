
这是一个使用 Rust 语言讲述数据结构与算法的教程系列文章。以下是每章的题目、内容、大节与小节。每次介绍一个数据结构或算法时，你需要使用 Rust 语言较为详实地写出是如何实现的，并且需要贯穿 Rust 语言本身的知识点。另一方面，你还需要结合现实情况说明当前这些结构或算法的应用情况，例如，在主流语言（如C++, JAVA, Rust）中，哈希表使用的哈希函数、哈希冲突解决方法等，排序算法选择哪一种等。最后，你可以适当给出一些数据结构的应用场景，例如哈夫曼树。

以下，=代表题目，==代表大节，===代表小节。但在你需要写出的文章中，题目和大节都使用=，小节使用==。在以下给你的指引中，大节之下可能没给你小节的指引，你可能需要自己添加。但大节需要严格按照我给你的指引。每个大节之间，要使用\#html.hr()来分隔。

你需要自行画图并插入。画图使用typst画出，画图文件存在 G:\\Code\\Typst\\画图 中。你需要自己编译输出图片文件。本机中有typst编译期，在 D:\\Program Files\\Typst 中。

你需要善于利用当前环境中有的 tufted 系列环境，例如theorem, remark 等。还有，一些一两句话就能说明的补充性文字，可以使用 footnote。

你不要学其他文章在标题之下就添加一个header.jpg的做法。图片是用在行文中的，要做出具体形式，而不是总结。

对于大节，你需要使用一、二、等编号。对于小节，你需要使用1. 2. 等编号。例如：
一、大节一
1. 小节一
2. 小节二

二、大节二
1. 小节一
2. 小节二

对于“延伸阅读”，你应当使用reference.bib的形式。

= 顺序表与链表，栈与队列

== 顺序表

== 链表

=== 单链表、双链表与循环链表

你需要给出这三种链表的 Rust 语言实现。显然，你在这里还需要讲清楚 Rust 语言中所有权、借用、生命周期等概念在链表实现中的应用。

=== 增删查改

这一小节都以双链表来说明。需要讲清楚边界条件。

==== 节点的遍历与查找

==== 节点的插入与删除

=== 进阶算法及变体

==== 快慢指针

==== 链表反转

==== 跳表

=== 当今链表的应用

你可以讲现在链表还用在什么地方。但是，还需要讲清楚为什么现在链表的应用越来越少了，例如内存与缓存的问题。

== 栈

借机向读者灌输“接口与实现分离”的工程思想。

=== 顺序栈与链式栈

=== 主流语言中栈的实现方式

== 队列

=== 顺序队列

=== 环形队列与链式队列

=== 变体

==== 双端队列

==== 优先队列

这里可能可以不用讲太细，但要为后续《树与堆》做铺垫。

==== 阻塞队列

=== 主流语言中队列的实现方式

== 总结

这里可以使用表格来对比。

---

= 哈希表

你需要注意：本章不仅要讲清楚HashMap，也要顺便讲HashSet。你不一定要详细讲HashSet。

== 哈希函数

== 负载因子与扩容

== 哈希冲突的解决

=== 链地址法

=== 开放地址法

需要讲清楚线性探测、二次探测、双重散列方法。

== 扩容策略

== 哈希攻击

== 主流语言中哈希表的实现方式

---

= 树与堆

== 树的基础概念

== 二叉树

=== 二叉树的相关概念

这里需要讲满二叉树、完全二叉树、完美二叉树等。需要讲清楚任何树都可以转换为二叉树。

=== 二叉树的实现

=== 二叉树的遍历

需要讲清楚前序、中序、后序、层序遍历。需要讲清楚递归与非递归的实现方式。还需要说明 DFS 与 BFS 。

=== 二叉搜索树

==== 性质

==== 增删查改

==== 复杂度

要引出平衡树。

== 堆

=== 堆的概念性质

需要说明大顶堆和小顶堆。说明和优先队列的关系。

=== 堆的算法

==== 上浮与下沉

==== 建堆

== AVL 树

要说明接下来的大节都是平衡树。接下来的大节你可以给出简单的代码实现，不需要完全实现。

=== AVL 树的性质

说明平衡因子。

=== 四种旋转

== 红黑树

要说明是工程中最常用的平衡树。

== B 树与 B+ 树

=== B 树

=== B+ 树

要说明 B+ 树在数据库索引中的应用。说明为什么数据库要使用这种树。

== 主流语言中平衡树的应用

---

= 排序算法

== 时间复杂度、空间复杂度与稳定性

解释排序算法中这三个概念及其影响。

== 冒泡、选择、插入排序

== 归并排序

== 快速排序

== 堆排序

== 希尔排序

== 计数排序

== 基数排序

== 桶排序

== 主流语言中排序算法的实现方式

=== IntroSort

=== TimSort

=== pdqSort

== 总结

需要说明介绍的排序算法的分类。你可以参考：

排序算法
├── 比较排序（基于元素比较）
│   ├── O(n²) 级
│   │   ├── 冒泡排序
│   │   ├── 选择排序
│   │   └── 插入排序
│   ├── O(n log n) 级
│   │   ├── 归并排序（分治）
│   │   ├── 快速排序（分治）
│   │   └── 堆排序（基于堆）
│   └── 特殊比较排序
│       └── 希尔排序（插入排序的改进）
└── 非比较排序（不直接比较元素）
    ├── 计数排序（Counting Sort）
    ├── 基数排序（Radix Sort）
    └── 桶排序（Bucket Sort）

---

= 查找算法

== 顺序查找与二分查找

这里需要讲清楚分别属于无序查找和有序查找。

=== 顺序查找

=== 二分查找

== 二叉搜索树查找

== 平衡树查找

== B 树 / B+ 树查找

== 哈希查找

== 主流语言中查找算法的实现方式

== 总结

可以参考：

查找算法
├── 无序查找（不依赖数据顺序）
│   └── 顺序查找（Sequential Search）—— O(n)
│
├── 有序查找（依赖数据有序）
│   └── 二分查找（Binary Search）—— O(log n)
│
├── 树形查找（利用树结构的查找）
│   ├── 二叉搜索树（BST）查找 —— O(log n) 平均
│   ├── 平衡树（AVL/红黑树）查找 —— O(log n) 严格
│   └── B 树 / B+ 树查找 —— 数据库索引的核心
│
└── 散列查找（哈希查找）
    └── 哈希表（Hash Table）查找 —— O(1) 平均

---

= 图与并查集

需要说明主流语言中都没有图以及并查集的标准实现。

== 图的基本概念

== 图的存储结构

=== 邻接矩阵

=== 邻接表

== 图的遍历

=== 深度优先搜索（DFS）

=== 广度优先搜索（BFS）

== 并查集

=== 并查集的基本概念与操作

=== 并查集的优化策略

==== 路径压缩（Path Compression）

==== 按秩合并（Union by Rank）

==== 复杂度

如果可以，证明复杂度是 $O(alpha (n))$，其中 $alpha (n)$ 是阿克曼函数的反函数。

=== 并查集的实现

== 拓扑排序

== 最短路径

=== Dijkstra 算法

=== Bellman-Ford 算法

=== Floyd-Warshall 算法

== 最小生成树

=== Prim 算法

=== Kruskal 算法

需要说明 Kruskal 算法需要用到并查集。

== 总结

---

= 字符串算法

注意本章需要多使用数学语言来表述。

== 字符串的定义

数学语言讲清楚什么是字符串，什么是子串，什么是前缀，什么是后缀，什么是最长公共前缀，什么是最长公共子串等。

== 字符串匹配

数学语言讲清楚什么是字符串匹配，什么是模式串，什么是文本串等。

=== 暴力匹配算法

=== Rabin-Karp 算法

=== KMP 算法

=== Boyer-Moore 算法

=== 双端匹配

=== 主流语言中字符串匹配算法的实现方式

== 多模式匹配

数学语言讲清楚什么是多模式匹配，什么是模式集合，什么是文本等。

=== Trie 树

=== AC 自动机

== 字符串哈希

== 字符串的数据结构

=== 前缀树

=== 后缀树

== 其他算法

=== 字典序

需要讲清楚什么是字典序，如何判断两个字符串的字典序大小，如何求一个字符串的字典序的下一个字符串。

=== 最长公共子串

=== 编辑距离

== 总结

---

= 高精度数与大数乘法

本章只讨论整数。

== 高精度数的表示与存储

需要讲清楚应该用什么存储方式存储大数，并说明优缺点。说明高位在前和低位在前如何择优。之后给出高精度数的 Rust 语言实现，类名为`BigInteger`。

== 高精度数的加减

需要讲清楚高精度数的加减法原理和实现方式。给出具体的 Rust 代码实现。

== 高精度数的乘法

=== 朴素乘法

给出代码实现，复杂度分析，并说明低效。

=== Karatsuba 乘法

给出代码实现，复杂度分析。

=== Toom-Cook 乘法

=== Fast Fourier Transform (FFT) 乘法

=== Schönhage-Strassen 乘法

== 主流语言中大数的实现方式

---
